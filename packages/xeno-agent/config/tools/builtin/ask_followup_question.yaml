name: ask_followup_question
description: "【询问用户】⚠️ 已弃用，请使用 Flow 级 HITL"
parameters:
  question:
    type: string
    description: The question to ask.
    required: true
  options:
    type: array
    items:
        type: string
    description: Optional list of valid answers.
    required: false
python_module: "xeno_agent.tools.meta_tools"
python_class: "AskFollowupTool"
metadata:
  category: meta
