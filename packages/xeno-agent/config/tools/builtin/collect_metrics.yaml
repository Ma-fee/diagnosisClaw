name: collect_metrics
description: Simulate collecting system metrics.
parameters:
  metric_types:
    type: string
    description: Types of metrics to collect (cpu, memory, errors, latency, all)
    default: "all"
    required: false
python_module: "xeno_agent.tools.diagnostic_tools"
python_class: "CollectMetricsTool"
metadata:
  category: functional
